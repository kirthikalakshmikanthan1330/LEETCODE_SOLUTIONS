# Write your MySQL query statement below
select p.project_id,round(ifnull(sum(experience_years)/count(p.project_id),0),2) as average_years from Project  p left join  employee e on p.employee_id=e.employee_id   group by p.project_id;